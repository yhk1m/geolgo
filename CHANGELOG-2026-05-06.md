# 2026-05-06 변경 사항

## 작업 목적
관리자가 보고한 두 가지 문제 해결:
1. 한 학생이 신청을 완료한 상태에서 다시 신청하면 그대로 접수되는 문제
2. 중복 신청 중 하나를 휴지통으로 보내면 수험번호에 빈 자리가 남고 자동으로 채워지지 않는 문제

## 변경 파일
- `src/lib/examNumber.ts`
- `src/app/register/individual/page.tsx`
- `src/app/register/group/page.tsx`

커밋: `354d6a5` (master)

---

## 1. 수험번호 빈 자리 자동 채움 (examNumber.ts)

### 변경 전
`computeExamNumbers`가 전체 신청 데이터에 대해 `created_at` 오름차순으로 자리를 부여했음.
- 휴지통(`payment_status === 'deleted'`)에 들어간 항목도 자리를 차지함
- 휴지통으로 옮긴 학생의 수험번호 자리가 그대로 남아 다음 활성 신청자에게 넘어가지 않음

### 변경 후
`computeExamNumbers` 진입 시 `payment_status !== 'deleted'`인 활성 신청만 필터링한 뒤 자리를 부여.
- 휴지통 항목은 자리 부여에서 제외됨 → 휴지통에 들어간 학생은 수험번호가 비어 보임 (`-`)
- 활성 신청자들은 `created_at` 순으로 빈 자리 없이 1번부터 채워짐

### 예시 (지역코드 03)
신규 D 신청 전:
- A (t1) → 11010103
- B (t2, 삭제) → `-` (자리 부여 안 됨)
- C (t3) → 11010203 ← 빈 자리 채움

D 신청 후:
- A (t1) → 11010103
- B (t2, 삭제) → `-`
- C (t3) → 11010203
- D (t4) → 11010303

---

## 2. 신청 중복 차단

### 검사 조건
**이름 + 생년월일 + 전화번호** 세 항목이 모두 동일한 활성 신청(휴지통 제외)이 DB에 있으면 신청을 차단하고 모달로 안내.
- 전화번호는 숫자만 추출(`replace(/[^0-9]/g, '')`)해서 비교 → 하이픈 유무 무관

### 개인 신청 (`register/individual/page.tsx`)
- 사진 첨부 검증 직후, Supabase 업로드 전에 검사
- 중복이면 `setDuplicateAlert(...)`로 모달 표시 후 `setSubmitting(false); return`
- 모달 메시지: "{이름}님은 이미 신청 내역이 있습니다... 신청 내역 확인은 '접수 확인' 메뉴에서 가능합니다."

### 단체 신청 (`register/group/page.tsx`)
사진 첨부 검증 직후, 두 단계로 검사:

**(1) 입력 목록 내부 중복**
같은 CSV/입력 안에 이름+생년월일+전화번호가 동일한 행이 두 번 들어있는지 검사.
사유 예: "입력 목록 2번과 5번이 동일 정보입니다."

**(2) DB 내 기존 신청과 중복**
참가자 이름들로 한 번에 조회한 뒤 (in 절), 각 참가자별로 생년월일·전화번호 일치 여부 확인.
사유 예: "이미 신청 내역이 존재합니다 (○○고등학교)."

중복이 하나라도 있으면 모달로 학생별 사유를 모두 보여주고 일괄 차단.

---

## 해결됨 (2026-05-10)

### 휴지통 복구 시 수험번호 부여 방식 → **맨 뒤에 붙이기**로 결정
복구 시 `created_at`을 복구 시각으로 갱신해 정렬상 맨 뒤로 가도록 수정.
- 결과: 기존 활성 신청자들의 수험번호가 변동되지 않음 → 입금 확인이 끝난 학생들의 수험표 안전성 확보
- 복구된 학생만 마지막 번호를 새로 받음

**적용 범위**:
- `payment_status: 'deleted' → 'pending'` (휴지통 복구)에만 적용
- `payment_status: 'cancelled' → 'pending'` (신청 취소 복구)는 기존 동작 유지 — `created_at` 변경 없음

**변경 파일**:
- `src/components/ParticipantManager.tsx` — `executeRestore()`에서 deleted/cancelled를 분리해 처리
- `src/lib/mockdata.ts` — `restoreMockRegistrations()`에서 `created_at` 갱신
