# Unigeo - 전국지리올림피아드 신청 시스템

## 프로젝트 개요

**Unigeo**는 제26회 전국지리올림피아드의 참가 신청 및 관리를 위한 웹 애플리케이션입니다.
학생 개인 또는 학교 단체로 참가 신청을 할 수 있으며, 관리자는 입금 확인 및 참가자 관리를 수행할 수 있습니다.

- **배포 URL:** https://geoolympiad.kr (https://geolgo.vercel.app)
- **GitHub:** https://github.com/yhk1m/geolgo
- **참가비:** 20,000원 (1인)
- **접수 기간:** 2026. 4. 27 ~ 5. 16
- **시험일:** 2026. 5. 23 (토) 14:00~15:30

---

## 기술 스택

| 분류 | 기술 |
|---|---|
| 프레임워크 | Next.js 16.1.6 (App Router) |
| 언어 | TypeScript 5 |
| UI | React 19.2.3 |
| 스타일링 | Tailwind CSS 4 |
| 데이터베이스 | Supabase (PostgreSQL) |
| 지도 시각화 | ECharts 6.0.0 + echarts-for-react |
| 엑셀 내보내기 | XLSX 0.18.5 |
| PDF 생성 | jsPDF 4.2.0 |
| 폰트 | Noto Sans KR (Google Fonts) |
| 배포 | Vercel |

---

## 프로젝트 구조

```
src/
├── app/
│   ├── admin/
│   │   ├── dashboard/page.tsx     # 관리자 대시보드
│   │   ├── payments/page.tsx      # 참가자 관리 (입금 확인)
│   │   ├── layout.tsx             # 관리자 인증 레이아웃
│   │   └── page.tsx               # /admin → dashboard 리다이렉트
│   ├── register/
│   │   ├── individual/page.tsx    # 개인 참가 신청
│   │   └── group/page.tsx         # 학교 단체 접수
│   ├── check/page.tsx             # 접수 확인 + 수험표 다운로드
│   ├── stats/page.tsx             # 지역별 신청 현황
│   ├── page.tsx                   # 메인 페이지
│   ├── layout.tsx                 # 루트 레이아웃
│   └── globals.css                # 전역 스타일
├── components/
│   ├── Navigation.tsx             # 상단 네비게이션
│   ├── Footer.tsx                 # 하단 푸터
│   └── RegionMap.tsx              # 지도 시각화 컴포넌트
├── lib/
│   ├── supabase.ts                # Supabase 클라이언트 설정
│   ├── mockdata.ts                # Mock 데이터 생성기
│   ├── regions.ts                 # 17개 광역자치단체 데이터
│   ├── examNumber.ts              # 수험번호 생성/관리
│   ├── examTicket.ts              # 수험표 PDF 생성
│   ├── pdfFont.ts                 # PDF 폰트 설정 (한글)
│   └── photoRoster.ts             # 사진명렬표 PDF 생성
public/
└── geo/
    └── korea_sido_final.geojson   # 대한민국 시도 GeoJSON
```

---

## 페이지별 기능

### 1. 메인 페이지 (`/`)

- **대회 일정**: 접수 기간, 대회일, 결과 발표일 (호버 시 검은 박스 효과)
- **대회 요강**: 대회 형식, 참가 대상, 참가비 안내
- **문의처**: 지역별 담당자 이메일 테이블 (17개 광역자치단체)

### 2. 개인 참가 신청 (`/register/individual`)

- 입력 항목: 이름, 학교명, 학년(1~3), 반, 지역, 전화번호, 이메일, 생년월일
- 증명사진 업로드 (이미지 파일, 최대 5MB)
- 지도교사 정보: 교사명, 전화번호, 이메일
- 개인정보 수집 동의 체크박스 (필수)
- Supabase `registrations` 테이블에 직접 INSERT
- 신청 완료 시 입금 계좌 안내 모달 표시

### 3. 학교 단체 접수 (`/register/group`)

- **지도교사 정보**: 교사명, 학교명, 연락처, 지역, 이메일
- **참가 학생 목록**: 이름, 학년, 반, 전화번호, 이메일, 생년월일
  - 학생 추가/삭제 버튼
  - CSV 파일 일괄 업로드 (양식 다운로드 제공)
  - 사진 일괄 업로드 (CSV 버튼 옆에 배치, 학생 이름 기준 매칭)
  - 개별 사진 업로드
  - 중복 이름 사진 충돌 해결 모달
- 총 참가비 자동 계산 (학생 수 × 20,000원)
- Supabase `groups` + `registrations` 테이블에 INSERT

### 4. 접수 확인 (`/check`)

- 이름 + 생년월일 + 전화번호로 검색
- 전화번호 하이픈 포함/미포함 모두 지원
- 결과: 학교, 학년, 지역, 접수 유형, 입금 상태, 참가비, 신청일
- 입금 확인 완료 시 수험표 PDF 다운로드 가능

### 5. 지역별 신청 현황 (`/stats`)

- **지도 시각화** (ECharts)
  - 단계구분도 (등개수/Quantile 분류, 5단계)
  - 실제 구간 수에 따라 색상 골고루 분배 (2구간→1,5단계 / 3구간→1,3,5단계)
  - 1~2단계(연한 색) 지역은 짙은 테두리(#999), 3~5단계는 옅은 테두리(#fff)
  - 지도 3요소: 방위표(북쪽 화살표), 축척(동적), 좌표(위경도 격자선)
  - 독도 마커 (경상북도 데이터 연동)
  - 호버 시 한글 지역명 라벨 표시
  - 클릭 시 노란색(#facc15) 선택 + 한글 라벨 + 하단 테이블 해당 행 노란색 하이라이트
  - 줌/팬 지원, 축척 동적 업데이트
- **보기 모드 4종**:
  - 지도 (신청자 수)
  - 지도 (만명당 신청자 수)
  - 막대 (신청자 수)
  - 막대 (만명당 신청자 수)
- **통계 테이블**
  - 컬럼: 순위, 지역, 신청자 수, 전체 고등학생 수*, 만명당 신청자 수, 비율
  - 모든 컬럼 정렬 가능 (desc → asc → default 순환)
  - 검은 배경 헤더
  - 비율 프로그레스 바
  - 지도 클릭 시 해당 지역 행 노란색 하이라이트
- 실시간 업데이트 (Supabase Realtime 구독)
- 하단 각주: 기준 데이터 출처 (교육통계서비스 KESS, 2025.04.01.)

### 6. 관리자 대시보드 (`/admin/dashboard`)

- 비밀번호 인증 (`admin0220`, sessionStorage 기반)
- **요약 카드**: 총 신청자, 입금 확인/대기, 확인된 금액, 개인/단체, 미확인 금액
- **지역별 신청 현황**: 프로그레스 바 목록
- **최근 신청 목록**: 최근 15명, 이름/학교/날짜/입금 상태
- 실시간 업데이트 (Supabase Realtime)

### 7. 참가자 관리 (`/admin/payments`)

- **탭 필터**: 전체 명단 / 입금 대기 / 입금 확인 / 휴지통
- **검색**: 이름, 학교, 지역으로 검색
- **정렬**: 접수 유형, 신청일 기준 정렬
- **페이지네이션**: 10 / 20 / 50건 단위
- **개별 관리**: 입금 확인 / 취소 버튼
- **일괄 관리**: 체크박스 선택 → 일괄 입금 확인/취소
- **엑셀 내보내기**: 전체 명단, 입금 대기, 입금 확인 별도 다운로드 (수험번호 포함)
- **상세 모달**: 수험번호, 이름, 생년월일, 전화번호, 이메일, 지도교사 정보, 수정 이력
- **참가자 정보 수정**: 이름, 학교, 학년, 반, 전화번호, 이메일, 생년월일, 지역, 사진 변경 (변경 이력 자동 기록)
- **시험장소 관리**: 지역별 학교명, 주소 설정
- **수험표 PDF**: 입금 확인된 참가자 일괄 다운로드
- **사진명렬표 PDF**: 지역별 생성 (진행률 표시 바)
- **삭제**: 소프트 삭제(휴지통) + 영구 삭제

---

## 수험번호 체계

### 형식 (총 8자리)

```
11 + NN(분반) + NN(번호) + NN(지역번호)
```

| 구성 | 자릿수 | 설명 |
|---|---|---|
| 접두사 | 2 | 고정값 `11` |
| 분반 | 2 | 01부터 시작, 분반당 최대 70명 |
| 번호 | 2 | 01~70 범위, 70 도달 시 분반 +1 |
| 지역번호 | 2 | 지역별 고유 코드 |

### 번호 부여 규칙

- 지역별로 `created_at` 순서대로 부여
- 분반당 최대 70명
- 70번 이후 → 다음 분반 01번으로 초기화

### 예시 (서울, 지역번호 01)

| 접수 순서 | 분반 | 번호 | 수험번호 |
|---|---|---|---|
| 1번째 | 01 | 01 | `11010101` |
| 70번째 | 01 | 70 | `11017001` |
| 71번째 | 02 | 01 | `11020101` |
| 140번째 | 02 | 70 | `11027001` |
| 141번째 | 03 | 01 | `11030101` |

### 지역번호 매핑

| 지역 | 코드 | 지역 | 코드 |
|---|---|---|---|
| 서울 | 01 | 광주 | 10 |
| 인천 | 02 | 전남 | 11 |
| 경기 | 03 | 전북 | 12 |
| 강원 | 04 | 대전 | 13 |
| 부산 | 05 | 세종 | 14 |
| 울산 | 06 | 충남 | 15 |
| 경남 | 07 | 충북 | 16 |
| 대구 | 08 | 제주 | 17 |
| 경북 | 09 | | |

---

## 데이터베이스 스키마

### `groups` 테이블

| 컬럼 | 타입 | 설명 |
|---|---|---|
| id | UUID (PK) | 그룹 ID |
| school_name | TEXT | 학교명 |
| teacher_name | TEXT | 지도교사명 |
| teacher_phone | TEXT | 교사 연락처 |
| teacher_email | TEXT (nullable) | 교사 이메일 |
| region | TEXT | 지역 (영문) |
| participant_count | INTEGER | 참가 학생 수 |
| created_at | TIMESTAMPTZ | 생성일시 |

### `registrations` 테이블

| 컬럼 | 타입 | 설명 |
|---|---|---|
| id | UUID (PK) | 등록 ID |
| name | TEXT | 학생 이름 |
| school | TEXT | 학교명 |
| grade | INTEGER | 학년 (1~3) |
| class_name | TEXT (nullable) | 반 |
| phone | TEXT | 전화번호 |
| email | TEXT (nullable) | 이메일 |
| birthdate | DATE (nullable) | 생년월일 |
| photo_url | TEXT (nullable) | 증명사진 URL |
| region | TEXT | 지역 (영문) |
| registration_type | TEXT | 'individual' 또는 'group' |
| group_id | UUID (FK, nullable) | 소속 그룹 ID |
| payment_status | TEXT | 'pending', 'confirmed', 'deleted' |
| payment_amount | INTEGER | 참가비 (기본 20,000) |
| teacher_name | TEXT (nullable) | 지도교사명 (개인접수) |
| teacher_phone | TEXT (nullable) | 교사 연락처 (개인접수) |
| teacher_email | TEXT (nullable) | 교사 이메일 (개인접수) |
| created_at | TIMESTAMPTZ | 신청일시 |

### `exam_locations` 테이블

| 컬럼 | 타입 | 설명 |
|---|---|---|
| region | TEXT (PK) | 지역 (영문) |
| school_name | TEXT | 시험장 학교명 |
| address | TEXT | 시험장 주소 |
| created_at | TIMESTAMPTZ | 생성일시 |
| updated_at | TIMESTAMPTZ | 수정일시 |

### `edit_logs` 테이블

| 컬럼 | 타입 | 설명 |
|---|---|---|
| id | UUID (PK) | 로그 ID |
| registration_id | UUID (FK) | 대상 등록 ID |
| changed_fields | JSONB | 변경된 필드 내역 |
| created_at | TIMESTAMPTZ | 변경일시 |

### Storage

- **`photos` 버킷** (public): 참가자 사진 파일 저장

### 정책 및 설정

- **RLS** 활성화: 공개 읽기/삽입, service role 업데이트
- **Realtime** 활성화: registrations 테이블 변경 구독

---

## 지도 시각화 (RegionMap)

### 주요 기능

- **GeoJSON 기반**: `korea_sido_final.geojson` (17개 광역자치단체)
- **단계구분도**: 등개수(Quantile) 분류, 5단계 색상
  - 실제 구간 수가 5개 미만일 경우 색상을 골고루 분배
  - 예: 2구간 → 1,5단계 색상 / 3구간 → 1,3,5단계 색상
- **테두리 차등 적용**: 1~2단계(연한 색) → 짙은 테두리(#999), 3~5단계 → 옅은 테두리(#fff)
- **지도 3요소**:
  - 방위표: 좌측 상단 북쪽 화살표
  - 축척: 우측 하단, 줌에 따라 동적 업데이트 (`convertToPixel` 기반)
  - 좌표: 위경도 격자선 (34°N~38°N, 126°E~130°E) - `markLine`으로 지도와 함께 이동
- **독도 표시**: `markPoint`로 (131.87°E, 37.24°N)에 표시, 경상북도 데이터 툴팁
- **호버**: 한글 지역명 라벨 표시 (emphasis)
- **클릭**: 노란색(#facc15) 선택 + 한글 라벨 + 하단 테이블 연동 (select, single 모드)
- **보기 모드 4종**: 지도(신청자 수) / 지도(만명당 신청자 수) / 막대(신청자 수) / 막대(만명당 신청자 수)
- **범례**: `selectedMode: false` (클릭 토글 비활성화)

### 색상 팔레트 (5단계)

```
#f0f0f0 → #cccccc → #888888 → #444444 → #000000
```

### Props

| Prop | 타입 | 설명 |
|---|---|---|
| data | `{ name: string; value: number }[]` | 지역별 데이터 |
| onRegionClick | `(regionName: string \| null) => void` | 지역 클릭 콜백 (선택 해제 시 null) |

---

## 17개 광역자치단체 데이터

| 코드 | 영문명 | 한글명 | 약어 | 수험번호 지역코드 | 고등학생 수 |
|---|---|---|---|---|---|
| 11 | Seoul | 서울특별시 | 서울 | 01 | 203,087 |
| 26 | Busan | 부산광역시 | 부산 | 05 | 70,832 |
| 27 | Daegu | 대구광역시 | 대구 | 08 | 59,533 |
| 28 | Incheon | 인천광역시 | 인천 | 02 | 75,891 |
| 29 | Gwangju | 광주광역시 | 광주 | 10 | 41,505 |
| 30 | Daejeon | 대전광역시 | 대전 | 13 | 38,605 |
| 31 | Ulsan | 울산광역시 | 울산 | 06 | 31,404 |
| 36 | Sejong-si | 세종특별자치시 | 세종 | 14 | 14,342 |
| 41 | Gyeonggi-do | 경기도 | 경기 | 03 | 360,932 |
| 43 | Chungcheongbuk-do | 충청북도 | 충북 | 16 | 40,497 |
| 44 | Chungcheongnam-do | 충청남도 | 충남 | 15 | 59,648 |
| 45 | Jeollabuk-do | 전북특별자치도 | 전북 | 12 | 48,191 |
| 46 | Jellanam-do | 전라남도 | 전남 | 11 | 45,721 |
| 47 | Gyeongsangbuk-do | 경상북도 | 경북 | 09 | 64,076 |
| 48 | Gyeongsangnam-do | 경상남도 | 경남 | 07 | 89,933 |
| 50 | Jeju-do | 제주특별자치도 | 제주 | 17 | 18,711 |
| 51 | Gangwon-do | 강원특별자치도 | 강원 | 04 | 36,558 |

- 기준: 광역자치단체별 만 15~17세 인구 (2025.04.01.)
- 출처: 교육통계서비스 KESS

---

## 모바일 반응형 디자인

### 공통

- Tailwind 브레이크포인트: `sm:` (640px), `md:` (768px)
- 모바일 우선 레이아웃 → 데스크톱 확장

### Navigation

- 데스크톱: 가로 메뉴 바
- 모바일: 햄버거 메뉴 (토글 드롭다운)
- 부제목 모바일 숨김

### 관리자 테이블 (참가자 관리) 모바일 최적화

| 항목 | 데스크톱 | 모바일 |
|---|---|---|
| 학교명 | 서울대학교사범대학부설고등학교 | 서울사대부고 |
| 지역 | 서울특별시 | 서울 |
| 금액 컬럼 | 표시 | 숨김 |
| 신청일 | 2026. 2. 21. | 2.21. |

### 학교명 약어 규칙 (모바일)

```
1. 특수 학교명 (정확히 일치)
   - 용인한국외국어대학교부설고등학교 → 외대부고
   - 이화여자대학교사범대학부속이화금란고등학교 → 이대부고
   - 이화여자외국어고등학교 → 이화외고

2. 사대부고 패턴
   - (국립)XX대학교사범대학부설/부속...고등학교 → XX사대부고

3. 외국어고 패턴
   - XX외국어고등학교 → XX외고

4. 여자고 패턴
   - XX여자고등학교 → XX여고

5. 기본
   - XX고등학교 → XX고
```

### 지역별 현황 테이블 모바일 최적화

| 항목 | 데스크톱 | 모바일 |
|---|---|---|
| 지역 | 서울특별시 | 서울 |
| 헤더 "신청자 수" | 신청자 수 | 신청 |
| 헤더 "만명당 신청자 수" | 만명당 신청자 수 | 만명당 |
| 전체 고등학생 수* 컬럼 | 표시 | 숨김 |
| 비율 프로그레스 바 | 표시 | 숨김 (퍼센트만) |

---

## 스타일 시스템

### 색상

| 용도 | 색상 |
|---|---|
| 주 텍스트 | #111111 |
| 보조 텍스트 | #666666 |
| 비활성 텍스트 | #999999 |
| 테두리 | #e5e5e5 |
| 카드 배경 | #fafafa |
| 강조 (경고) | #c00000 |
| 버튼/헤더 | #111111 |
| 호버 효과 박스 테두리 | #c8c8c8 (1.5px) |
| 지도 선택 (클릭) | #facc15 (노란색) |
| 테이블 선택 행 | bg-yellow-100 |

### 컴포넌트 클래스

```css
.btn           /* 버튼 기본 */
.btn-primary   /* 검은 배경 버튼 */
.btn-secondary /* 흰 배경 테두리 버튼 */
.badge         /* 상태 뱃지 기본 */
.badge-pending    /* 입금 대기 (회색) */
.badge-confirmed  /* 입금 확인 (검정) */
```

---

## Mock 데이터 시스템

Supabase가 설정되지 않은 환경(`isSupabaseConfigured === false`)에서 자동 활성화됩니다.

- **380건**의 Mock 등록 데이터 자동 생성
- 지역별 가중치 분배 (서울 72, 경기 65, 부산 28 등)
- 지역별 실제 고등학교 이름 사용
- 랜덤 한국인 이름 생성
- 65% 입금 확인, 35% 입금 대기
- 싱글턴 캐싱 (세션 내 일관성 유지)
- `updateMockPaymentStatus()` 지원 (관리자 페이지 테스트용)

### 테스트용 Mock 데이터 (CSV + 사진)

`mock_data/` 디렉토리에 17개 지역별 단체접수 테스트 데이터:
- 총 197명, 지역별 6~25명 분배
- CSV 형식: `이름,학년,반,전화번호,생년월일,이메일`
- 사진 파일: `학생이름.png` (학생별 1x1 PNG)
- 이메일: 영문 로마자 변환 (예: `kimseojun42@gmail.com`)

---

## 워크플로우

### 참가 신청 흐름

```
학생/교사 → 개인/단체 신청 → Supabase 저장 → 입금 안내 표시
→ 참가비 입금 (국민은행 477401-01-176602)
→ 관리자 입금 확인 → 접수 완료
→ 접수 확인 페이지에서 수험표 PDF 다운로드
```

### 관리자 워크플로우

```
비밀번호 로그인 → 대시보드 확인 → 참가자 관리
→ 입금 확인/취소 (개별 또는 일괄)
→ 엑셀 내보내기 (전체/대기/확인, 수험번호 포함)
→ 시험장소 설정
→ 수험표 PDF 일괄 생성
→ 사진명렬표 PDF 지역별 생성
```

---

## Footer

```
신청 시스템 문의 : bgmlkim@gmail.com
Copyright 2026. 양정고등학교 김용현. All Rights Reserved.
```

---

## 입금 안내

| 항목 | 내용 |
|---|---|
| 은행 | 국민은행 |
| 계좌번호 | 477401-01-176602 |
| 예금주 | (사)대한지리학회 |
| 개인 입금자명 | 소속고 이름 (예: ○○고 김○○) |
| 단체 입금자명 | 학교명 (예: ○○고) |
| 참가비 | 1인 20,000원 |

---

## Supabase 계정 이관 이력

### 2026-03-11: Supabase 프로젝트 이관

| 항목 | 이전 | 이후 |
|---|---|---|
| 프로젝트 ID | `abcrkyekmhulqniyujpf` | `umwoeqwbvxeygmdwninc` |
| URL | `https://abcrkyekmhulqniyujpf.supabase.co` | `https://umwoeqwbvxeygmdwninc.supabase.co` |

#### 이관 절차

1. 새 Supabase 프로젝트에서 `supabase_schema.sql` 실행 (테이블 4개 + RLS + 인덱스 + Realtime)
2. Storage에서 `photos` 버킷 생성 (public)
3. `.env.local` 환경변수 업데이트 (URL + Anon Key)
4. Vercel 환경변수 업데이트 + Redeploy

- 빈 DB로 새로 시작 (기존 데이터 미이관)

#### 환경변수 설정 위치

| 환경 | 위치 |
|---|---|
| 로컬 개발 | `.env.local` |
| 프로덕션 (Vercel) | Vercel Dashboard → Settings → Environment Variables |
